// The one and only game theme — mirrors GAME_PROMPT in backend/server.py.
export const GAME_PROMPT = "The most chaotic pic";

// Steers players toward pics that don't need a face to be guessed —
// the vibe gives them away, not their identity.
export const PROMPT_HINT =
  "A cursed screenshot, a meme you saved at 3AM, a blurry accident, a food crime — anything on your phone that screams chaos. No face needed. Let them guess whose mess it is.";
